<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.7.6.1">
  <compounddef id="uriresolvers" kind="page">
    <compoundname>uriresolvers</compoundname>
    <title>URI Resolvers</title>
    <detaileddescription>
<sect1 id="uriresolvers_1motivation">
<title>Motivation</title>
<para>In JSONiq or XQuery, many resources are named by URIs - most notably schemas and modules, but also documents, full-text stopwords lists and thesauri, and so on. URIs are by convention often given with the <computeroutput>http:</computeroutput> scheme and the domain name of the provider. However, in general it is not desirable to load the resource via HTTP, and in many cases the resource is not actually available at the named URI - in other words, they are <bold>URIs</bold> (Uniform Resource <bold>Identifiers</bold>), not <bold>URLs</bold> (Uniform Resource <bold>Locators</bold>).</para><para>Zorba provides a built-in mechanism for mapping URIs to a local filesystem path. It can also load resources via HTTP in situations where that is in fact appropriate. These built-in mechanisms will solve a large number of resource-loading problems. However, it also offers a highly flexible API for extending the built-in mechanisms, which will allow you to resolve URIs from queries in almost any way imaginable to handle application-specific problems.</para></sect1>
<sect1 id="uriresolvers_1builtin_uri_resolver">
<title>Zorba&apos;s Built-in URI Resolver</title>
<para>JSONiq or XQuery itself offer an approach to locating resources via the &quot;at&quot; clause in <computeroutput>import module</computeroutput> and <computeroutput>import schema</computeroutput> statements. However, we recommend that you do not use this approach for most robust applications.</para><para>For example, the following code snippet imports a library module with target namespace &quot;http://www.example.com/modules/utils&quot;.</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/>(:<sp/>Evil<sp/></highlight><highlight class="keyword">import</highlight><highlight class="normal"><sp/>statement<sp/>:)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="keyword">import</highlight><highlight class="normal"><sp/>module<sp/></highlight><highlight class="keyword">namespace</highlight><highlight class="normal"><sp/>utils<sp/>=<sp/></highlight><highlight class="stringliteral">&quot;http://www.example.com/modules/utils&quot;</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/>at<sp/></highlight><highlight class="stringliteral">&quot;/home/foo/xquery/utils.xq&quot;</highlight><highlight class="normal">;</highlight></codeline>
</programlisting></para><para>In this import statement, the user specifies that the file containing the module is physically located at &quot;/home/foo/xquery/utils.xq&quot;.</para><para>Having the physical location hard-coded isn&apos;t desirable for two reasons: First, the physical location of a module or schema may change during the development process; also, frequently the module will be located in a different directory after the application has been deployed. Second, the user may want to package and distribute the application without having other developers to perform code changes in order to have location hints which are valid on their system.</para><para>Out of the box, Zorba will attempt to map URIs to local filesystem locations. It does so in two steps:</para><para><orderedlist>
<listitem>
<para>First, the URI is <emphasis>transformed</emphasis> to a <emphasis>relative path</emphasis> via the algorithm listed below.</para><para></para></listitem>
<listitem>
<para>Second, Zorba attempts to load the relative path within every directory listed on its <emphasis>URI path</emphasis>. (Through this documentation, a &quot;path&quot; is defines as "an ordered list of directories in the filesystem.)</para><para></para></listitem>
</orderedlist>
</para><sect2 id="uriresolvers_1mangling">
<title>URI Transformation</title>
<para>Consider a library module with the namespace <ulink url="http://www.example.com/modules/utils">http://www.example.com/modules/utils</ulink>. This namespace URI will be rewritten by the following steps:</para><para><orderedlist>
<listitem>
<para>The domain (authority) component of the URI is reversed, and then transformed into a relative filename separated by forward slashes: <computeroutput>www.example.com</computeroutput> =&gt; <computeroutput>com/example/www</computeroutput></para><para></para></listitem>
<listitem>
<para>The path component of the URI is appended: <computeroutput>/modules/utils</computeroutput></para><para></para></listitem>
<listitem>
<para>If the path component ends with a trailing slash, the word &quot;index&quot; will be appended</para><para></para></listitem>
<listitem>
<para>Finally, an appropriate filename extension will be added if it is not already present. For modules, the suffix is <computeroutput>.xq</computeroutput>, and for schemas, the suffix is <computeroutput>.xsd</computeroutput>.</para><para></para></listitem>
</orderedlist>
</para><para>So, the final relative path in our example would be <computeroutput>com/example/www/modules/utils.xq</computeroutput>. A couple more examples (all are presumed to be module URIs):</para><para><itemizedlist>
<listitem>
<para><computeroutput><ulink url="http://www.example.com/modules/utils/">http://www.example.com/modules/utils/</ulink></computeroutput> =&gt; <computeroutput>com/example/www/modules/utils/index.xq</computeroutput></para><para></para></listitem>
<listitem>
<para><computeroutput><ulink url="http://www.example.com/modules/mylib.xq">http://www.example.com/modules/mylib.xq</ulink></computeroutput> =&gt; <computeroutput>com/example/www/modules/mylib.xq</computeroutput></para><para></para></listitem>
</itemizedlist>
</para></sect2>
<sect2 id="uriresolvers_1uripath">
<title>Zorba&apos;s URI Path</title>
<para>Zorba will now attempt to find the named file relative to each directory in the <emphasis>URI Path</emphasis>. By default, the only entry on the URI path is the directory <computeroutput>/usr/share/zorba/uris/</computeroutput> (on Unix and MacOS X) or <computeroutput>C:\Program Files\Zorba 2.1.0\share\zorba\uris</computeroutput> (on Windows). So, to complete this example, assuming Zorba is installed on a Unix system in the default location, Zorba will attempt to resolve the module namespace URI <computeroutput><ulink url="http://www.example.com/modules/utils">http://www.example.com/modules/utils</ulink></computeroutput> to the file</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/>/usr/share/zorba/modules/com/example/www/modules/utils.xq</highlight></codeline>
</programlisting></para><para>If you have modules or schemas installed in other locations on your system, you may provide additional search directories either by passing the <computeroutput>--uri-path</computeroutput> command-line argument to Zorba, or by setting the <computeroutput>ZORBA_URI_PATH</computeroutput> environment variable. In both cases, the value is an ordered list of filesystem directories separated by &quot;:&quot; (on Unix/MacOS X) or &quot;;&quot; (on Windows). Zorba will search each directory on the URI path in the order specified, and the first match found will be used. So, for example, if you invoke Zorba as follows (example is for a Unix system):</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/>zorba<sp/>--uri-path<sp/></highlight><highlight class="stringliteral">&apos;/home/foo/xquery/uris:/opt/share/xquery/uris&apos;</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/>-q<sp/></highlight><highlight class="stringliteral">&apos;import<sp/>module<sp/>namespace<sp/>utils=&quot;http://www.example.com/modules/utils&quot;;<sp/>1&apos;</highlight></codeline>
</programlisting></para><para>Zorba will attempt to load the module from the following locations, in order:</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/>/home/foo/xquery/uris/com/example/www/modules/utils.xq</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>/opt/share/xquery/uris/com/example/www/modules/utils.xq</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>/usr/share/zorba/uris/com/example/www/modules/utils.xq</highlight></codeline>
</programlisting></para><para>If, after searching all URI path directories, no match is found for a given URI, Zorba will by default fall back to interpretting the URI as a URL and loading the resource via HTTP / HTTPS / FTP (depending on the URI scheme). This behaviour can be defeated by disabling the <computeroutput>http-uri-resolution</computeroutput> Zorba option (see <ref refid="options_and_annotations_1options_features" kindref="member">Enabling or Disabling Features</ref>).</para></sect2>
</sect1>
<sect1 id="uriresolvers_1libpath">
<title>Zorba&apos;s Library Path</title>
<para>The above URI path mechanism is used for all URIs that are resolved by Zorba - most especially module and schema imports, but also full-text thesaurus and stop-word lists, documents, and so on.</para><para>When considering modules in particular, however, there is another important path: the Library Path. Zorba will look on this path when it needs to load dynamic libraries containing the implementation of external functions for a module (ie, those functions implemented in C++; see <ref refid="modules_authoring_2_1mod_author_cpp" kindref="member">External Functions in C++</ref>). This path is separate from the URI path because on certain systems - most notably Fedora, but also other Linux distributions - it is important that platform-dependent binary files (such as dynamic libraries) be installed in a separate directory structure from platform-independent files (such as <computeroutput>.xq</computeroutput> module files and <computeroutput>.xsd</computeroutput> schema files).</para><para>The Library Path mechanism is exactly parallel to the URI Path mechanism. The default, built-in entry on this path is <computeroutput>/usr/lib/zorba</computeroutput> (on Unix and MacOS X) and <computeroutput>C:\Program Files\Zorba 2.1.0\lib\zorba</computeroutput> (on Windows). You can add directories to this path using the <computeroutput>--lib-path</computeroutput> command-line argument, or by setting the <computeroutput>ZORBA_LIB_PATH</computeroutput> environment variable.</para></sect1>
<sect1 id="uriresolvers_1internalpaths">
<title>Internal (Core) Paths</title>
<para>There is actually one additional built-in directory on Zorba&apos;s URI Path, and one additional built-in directory on Zorba&apos;s Library Path, in addition to the default values mentioned above. These directories hold Zorba&apos;s built-in &quot;core&quot; modules (see <ref refid="modules_top_1core_modules" kindref="member">Core Modules</ref>). The directories are subdirectories of the default directory named &quot;core/ZORBA_VERSION&quot;. So for example, for Zorba 2.1.0 on Unix, the default internal URI directory is <computeroutput>/usr/share/zorba/uris/core/2.1.0</computeroutput> and the default internal library directory is <computeroutput>/usr/lib/zorba/core/2.1.0</computeroutput>.</para><para>These directories are separate to make it easier to upgrade Zorba, or support multiple installed versions of Zorba, while allowing non-core modules to be installed and versioned indepedently of the Zorba version. Normally you should not modify the contents of these directories.</para></sect1>
<sect1 id="uriresolvers_1configurepaths">
<title>Changing the Default Paths</title>
<para>All four paths mentioned above - the core and non-core URI Path, and the core and non-core Library Path - have compiled-in default values as discussed. All four of these default values can be modified at Zorba build time to meet your environment&apos;s requirements. You can do this by specifying alternate values for the following CMake variables:</para><para><programlisting><codeline><highlight class="normal">ZORBA_NONCORE_URI_DIR</highlight></codeline>
<codeline><highlight class="normal">ZORBA_CORE_URI_DIR</highlight></codeline>
<codeline><highlight class="normal">ZORBA_NONCORE_LIB_DIR</highlight></codeline>
<codeline><highlight class="normal">ZORBA_CORE_LIB_DIR</highlight></codeline>
</programlisting></para><para>Note that these are <emphasis>relative</emphasis> directories, and will be resolved relative to <computeroutput>CMAKE_INSTALL_PREFIX</computeroutput>.</para></sect1>
<sect1 id="uriresolvers_1modulepath">
<title>Zorba&apos;s &quot;Module Path&quot;</title>
<para>Earlier versions of Zorba had a single path for specifying where both platform-dependent library files and platform-independent module and schema files were located. This was somewhat inaccurately named the &quot;module path&quot;. For backwards compatibility, Zorba still supports a <computeroutput>--module-path</computeroutput> command-line argument and a <computeroutput>ZORBA_MODULE_PATH</computeroutput> environment variable (and the C++ API has a <computeroutput>StaticContext::setModulePaths()</computeroutput> method). Specifying a set of directories as the &quot;module path&quot; using any of these mechanisms is exactly the same as specifying that set of directories as both the URI path and library path.</para><para>The Module Path is deprecated, and these mechanisms may be removed in a future major version of Zorba. There is no &quot;default module path&quot;.</para></sect1>
<sect1 id="uriresolvers_1cplusplus">
<title>C++ API for URI Resolving</title>
<sect2 id="uriresolvers_1cppuripath">
<title>Modifying the URI Path programmatically</title>
<para>The simplest modification to Zorba&apos;s default behavior is setting the URI path programmatically. This allows you to have different URI paths per static context, if you wish.</para><para>The <computeroutput>StaticContext</computeroutput> C++ API class provides the <computeroutput>setURIPath()</computeroutput> method for this purpose. It is passed a vector of <computeroutput><ref refid="classzorba_1_1String" kindref="compound">zorba::String</ref></computeroutput> values, each being an absolute directory to add to the URI path.</para><para>For example, the following code snippet creates a <computeroutput>StaticContext</computeroutput> object; adds two directories to the URI path component of this static context; and compiles and executes a query given the information that is present in this static context (passed as the second parameter to the <computeroutput>compileQuery()</computeroutput> method).</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="comment">//<sp/>Create<sp/>a<sp/>new<sp/>static<sp/>context</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><ref refid="classzorba_1_1SmartPtr" kindref="compound">zorba::StaticContext_t</ref><sp/>staticCtx<sp/>=<sp/>zorba-&gt;createStaticContext();</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="comment">//<sp/>Set<sp/>the<sp/>URI<sp/>Path</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>std::vector&lt;zorba::String&gt;<sp/>uriPath(2);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>uriPath[0]<sp/>=<sp/></highlight><highlight class="stringliteral">&quot;/home/foo/xquery/uris&quot;</highlight><highlight class="normal">;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>uriPath[1]<sp/>=<sp/></highlight><highlight class="stringliteral">&quot;/opt/share/xquery/uris&quot;</highlight><highlight class="normal">;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>staticCtx-&gt;setURIPath(uriPath);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="comment">//<sp/>Compile<sp/>a<sp/>query<sp/>using<sp/>the<sp/>static<sp/>context<sp/>created<sp/>above</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><ref refid="classzorba_1_1SmartPtr" kindref="compound">zorba::XQuery_t</ref><sp/>query<sp/>=<sp/>zorba-&gt;compileQuery(</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="stringliteral">&quot;import<sp/>module<sp/>namespace<sp/>m=&apos;http://example.com/module&apos;;<sp/>m:foo()&quot;</highlight><highlight class="normal">,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>staticCtx);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="comment">//<sp/>execute<sp/>the<sp/>compiled<sp/>query<sp/>printing<sp/>the<sp/>result<sp/>to<sp/>standard<sp/>out</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>std::cout<sp/>&lt;&lt;<sp/>query<sp/>&lt;&lt;<sp/>std::endl;</highlight></codeline>
</programlisting></para></sect2>
<sect2 id="uriresolvers_1cpplibpath">
<title>Modifying the Library Path programmatically</title>
<para>Similarly, <computeroutput>StaticContext</computeroutput> has a method named <computeroutput>setLibPath()</computeroutput> for specifying the Library Path to use. In operation it behaves exactly like <computeroutput>setURIPath()</computeroutput>.</para></sect2>
<sect2 id="uriresolvers_1mapresolve">
<title>URI Mappers and URL Resolvers</title>
<para>Now we will discuss more advanced techniques for manipulating Zorba&apos;s URI resolution mechanism. The built-in mechanisms described above are in fact implemented internally by using these same techniques.</para><para>There are two types of class that you may implement to modify the URI resolution process:</para><para><orderedlist>
<listitem>
<para>A <emphasis>URI Mapper</emphasis> takes a URI and returns <emphasis>one or more</emphasis> &quot;Candidate URIs&quot;, which are alternative URIs Zorba will attempt to resolve. This allows you to change the URI, or provide several different potential URIs, for Zorba to resolve.</para><para></para></listitem>
<listitem>
<para>A <emphasis>URL Resolver</emphasis> takes a URI (which is presumed to be a URL, that is, a URI which actually points to a resource) and returns a <computeroutput>Resource</computeroutput> object which Zorba will use to load the resource data.</para><para></para></listitem>
</orderedlist>
</para><para>The general algorithm used by Zorba when resolving a URI is as follows:</para><para>Zorba will start by passing the original URI to the first registered URI Mapper. If that Mapper returns any candidate URIs, then Zorba will pass <emphasis>each</emphasis> such URI to the next registered URI Mapper, and so on. If any Mapper does not return any candidate URIs for a given input URI, Zorba will simply pass the input URI unchanged to the next Mapper.</para><para>After all Mappers have been invoked, Zorba will have a set of candidate URIs. It will then pass each candidate URI, in order, to each registered URL Resolver. The first time a URL Resolver returns a Resource object, Zorba will use that Resource as the final source of content for the URI. If no URL Resolver ever returns a Resource, then Zorba will raise an appropriate &quot;resource not found&quot; error.</para><sect3 id="uriresolvers_1urimappers">
<title>URI Mappers</title>
<para>Zorba includes a few built-in URI Mappers. For instance, the mechanism which iterates through the URI path and produces a set of filesystem files where the URI might be located is implemented as a URI Mapper.</para><para>To implement your own URI Mapper, subclass the C++ API class <computeroutput>URIMapper</computeroutput> and implement the <computeroutput>mapURI()</computeroutput> method:</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="keyword">virtual</highlight><highlight class="normal"><sp/></highlight><highlight class="keywordtype">void</highlight><highlight class="normal"><sp/>mapURI(</highlight><highlight class="keyword">const</highlight><highlight class="normal"><sp/><ref refid="classzorba_1_1String" kindref="compound" tooltip="The Zorba string class.">zorba::String</ref><sp/>aUri,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/>EntityData<sp/></highlight><highlight class="keyword">const</highlight><highlight class="normal">*<sp/>aEntityData,<sp/>std::vector&lt;zorba::String&gt;&amp;<sp/>oUris);</highlight></codeline>
</programlisting></para><para>and then register an instance of your subclass with the static context using the method <computeroutput>registerURIMapper()</computeroutput>:</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/><ref refid="namespacezorba_1a24d7241f6bf20badc1017264d30b2af5" kindref="member">StaticContext_t</ref><sp/>lContext<sp/>=<sp/>aZorba-&gt;<ref refid="classzorba_1_1Zorba_1a9c2f950f6c68a948683ae15d587d756c" kindref="member" tooltip="Creates a new StaticContext.">createStaticContext</ref>();</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>MyURIMapperSubclass*<sp/>lMapper<sp/>=<sp/></highlight><highlight class="keyword">new</highlight><highlight class="normal"><sp/>MyURIMapperSubclass();</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>lContext-&gt;registerURIMapper(lMapper);</highlight></codeline>
</programlisting></para><para>Note that the memory ownership of the <computeroutput>URIMapper</computeroutput> instance remains with the client program; it must de-allocate it appropriately when the static context is no longer used.</para><para>In your <computeroutput>mapURI()</computeroutput> implementation, <computeroutput>aUri</computeroutput> is the input URI. <computeroutput>aEntityData</computeroutput> is a pointer to additional information about the URI being resolved. As of Zorba 2.0, the only method on <computeroutput>EntityData</computeroutput> is <computeroutput>getKind()</computeroutput>, which will return an enumerated value describing what kind of URI is being resolved: SCHEMA, MODULE, THESAURUS, STOP_WORDS, COLLECTION, or DOCUMENT.</para><para><computeroutput>oUris</computeroutput> is where you place any candidate URIs, by calling the method <computeroutput>push_back()</computeroutput>. (You should not look at any existing contents of the vector.) If you push any candidate URIs onto the vector, Zorba will <emphasis>replace</emphasis> the input URI with the set of candidate URIs you provide. That means that if you want Zorba to consider the original URI <emphasis>in addition to</emphasis> the alternative URIs you provide, you must push the original URI onto the vector as well.</para><para>The <computeroutput>mapURI()</computeroutput> method should not throw any exceptions.</para><para>As a limited but functional example, here is a full class which will change the URI of a specific schema to an alternative URI. You could use this, for example, if you have a large body of XQueries using a particular schema that you do want Zorba to download from the web (hence the built-in filesystem mapping mechanism is not appropriate), but the URI that the schema is available from has changed and you do not wish to modify all the <computeroutput>import schema</computeroutput> statements.</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="keyword">class<sp/></highlight><highlight class="normal">MySchemaURIMapper<sp/>:<sp/></highlight><highlight class="keyword">public</highlight><highlight class="normal"><sp/>URIMapper</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">public</highlight><highlight class="normal">:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">virtual</highlight><highlight class="normal"><sp/>~MySchemaURIMapper()<sp/>{}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">virtual</highlight><highlight class="normal"><sp/></highlight><highlight class="keywordtype">void</highlight><highlight class="normal"><sp/>mapURI(</highlight><highlight class="keyword">const</highlight><highlight class="normal"><sp/><ref refid="classzorba_1_1String" kindref="compound" tooltip="The Zorba string class.">zorba::String</ref><sp/>aUri,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>EntityData<sp/></highlight><highlight class="keyword">const</highlight><highlight class="normal">*<sp/>aEntityData,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>std::vector&lt;zorba::String&gt;&amp;<sp/>oUris)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keywordflow">if</highlight><highlight class="normal"><sp/>(aEntityData-&gt;getKind()<sp/>!=<sp/>EntityData::SCHEMA)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keywordflow">return</highlight><highlight class="normal">;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keywordflow">if</highlight><highlight class="normal">(aUri<sp/>==<sp/></highlight><highlight class="stringliteral">&quot;http://www.example.com/helloworld&quot;</highlight><highlight class="normal">)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>oUris.push_back(</highlight><highlight class="stringliteral">&quot;http://example.com/schemas/helloworld.xsd&quot;</highlight><highlight class="normal">);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>};</highlight></codeline>
</programlisting></para><para>Note that the first thing <computeroutput>mapURI()</computeroutput> does is check that the URI being resolved is in fact for a schema. This is generally good practice to prevent surprises in the off-chance that the same URI is also used to identify some other kind of resource.</para></sect3>
<sect3 id="uriresolvers_1urlresolvers">
<title>URL Resolvers</title>
<para>Zorba includes two built-in URL Resolvers: One which handles <computeroutput>file:</computeroutput> URLs, and one which handles <computeroutput>http:</computeroutput>, <computeroutput>https:</computeroutput>, and <computeroutput>ftp:</computeroutput> URLs. These are implemented by using Zorba&apos;s <computeroutput>file</computeroutput> and <computeroutput>http-client</computeroutput> modules, respectively.</para><para>To implement your own URL Resolver, subclass the C++ API class <computeroutput>URLResolver</computeroutput> and implement the <computeroutput>resolveURL()</computeroutput> method:</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="keyword">virtual</highlight><highlight class="normal"><sp/>Resource*<sp/>resolveURL(</highlight><highlight class="keyword">const</highlight><highlight class="normal"><sp/><ref refid="classzorba_1_1String" kindref="compound" tooltip="The Zorba string class.">zorba::String</ref>&amp;<sp/>aUrl,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/>EntityData<sp/></highlight><highlight class="keyword">const</highlight><highlight class="normal">*<sp/>aEntityData);</highlight></codeline>
</programlisting></para><para>and then register an instance of your subclass with the static context using the method <computeroutput>registerURLResolver()</computeroutput>:</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/><ref refid="namespacezorba_1a24d7241f6bf20badc1017264d30b2af5" kindref="member">StaticContext_t</ref><sp/>lContext<sp/>=<sp/>aZorba-&gt;<ref refid="classzorba_1_1Zorba_1a9c2f950f6c68a948683ae15d587d756c" kindref="member" tooltip="Creates a new StaticContext.">createStaticContext</ref>();</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>MyURLResolverSubclass*<sp/>lResolver<sp/>=<sp/></highlight><highlight class="keyword">new</highlight><highlight class="normal"><sp/>MyURLResolverSubclass();</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>lContext-&gt;registerURLResolver(lResolver);</highlight></codeline>
</programlisting></para><para>You will note that this mechanism is exactly parallel to the URI Mapper mechanism. Also, as with URI Mappers, the memory ownership of the <computeroutput>URLResolver</computeroutput> instance remains with the client program; it must de-allocate it appropriately when the static context is no longer used.</para><para>In your <computeroutput>resolveURL()</computeroutput> method, the <computeroutput>aUri</computeroutput> and <computeroutput>aEntityData</computeroutput> arguments have exactly the same meanings as they do for <computeroutput>mapURI()</computeroutput>.</para><para>If your code recognizes the URL and wishes to return content for it, it must return a newly-allocated instance of some subclass of the abstract class <computeroutput>Resource</computeroutput>. In Zorba 2.0&apos;s public API, there is only one such subclass, <computeroutput>StreamResource</computeroutput>, which wraps around a <computeroutput>std::istream</computeroutput>.</para><para>It is important that all URL Resolvers check the <computeroutput>EntityData</computeroutput>&apos;s Kind and only return Resources for the appropriate kind of URIs, for two reasons:</para><para><orderedlist>
<listitem>
<para>They must return an appropriate subclass of <computeroutput>Resource</computeroutput> depending on the entity kind. Zorba is prepared to accept <computeroutput>StreamResource</computeroutput> for schemas, modules, stop-word lists, thesauri, and documents, but not for collections. In future, there will likely be additional resource subclasses and additional entity kinds, and returning an inappropriate resource subclass for a given entity type will have negative consequences.</para><para></para></listitem>
<listitem>
<para>They must return a resource which produces data appropriate for the kind of entity being resolved. For instance, when resolving a schema or document URI, Zorba expects the resource to produce well-formed XML. When resolving a module URI, Zorba expects the resource to produce an XQuery library module. Returning a resource which outputs incorrect data will result in errors.</para><para></para></listitem>
</orderedlist>
</para><para>As a fairly silly but functional example, here is a URL Resolver that returns a small hard-coded module for a specific URL:</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="keyword">using<sp/>namespace<sp/></highlight><highlight class="normal">zorba;</highlight></codeline>
<codeline><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="keyword">static</highlight><highlight class="normal"><sp/></highlight><highlight class="keywordtype">void</highlight><highlight class="normal"><sp/>streamReleaser(std::istream*<sp/>aStream)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">delete</highlight><highlight class="normal"><sp/>aStream;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="keyword">class<sp/></highlight><highlight class="normal">FoobarModuleURLResolver<sp/>:<sp/></highlight><highlight class="keyword">public</highlight><highlight class="normal"><sp/><ref refid="classzorba_1_1URLResolver" kindref="compound" tooltip="Interface for URL resolving.">URLResolver</ref></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">public</highlight><highlight class="normal">:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">virtual</highlight><highlight class="normal"><sp/>~FoobarModuleURLResolver()<sp/>{}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">virtual</highlight><highlight class="normal"><sp/><ref refid="classzorba_1_1Resource" kindref="compound" tooltip="The class representing the result of URL resolution.">Resource</ref>*<sp/>resolveURL(</highlight><highlight class="keyword">const</highlight><highlight class="normal"><sp/><ref refid="classzorba_1_1String" kindref="compound" tooltip="The Zorba string class.">String</ref>&amp;<sp/>aUrl,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><ref refid="classzorba_1_1EntityData" kindref="compound" tooltip="The class containing data which may be of use to URIMappers and URLResolvers when mapping/resolving a...">EntityData</ref><sp/></highlight><highlight class="keyword">const</highlight><highlight class="normal">*<sp/>aEntityData)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="comment">//<sp/>we<sp/>have<sp/>only<sp/>one<sp/>module</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keywordflow">if</highlight><highlight class="normal"><sp/>(aEntityData-&gt;<ref refid="classzorba_1_1EntityData_1a28e3e16623b5d77a68d1849d6a22533b" kindref="member" tooltip="Return the Kind of Entity for which this URI is being resolved.">getKind</ref>()<sp/>==<sp/><ref refid="classzorba_1_1EntityData_1a12bf89226b6bd93b9bf5f56343c0c18aa5b5cce2ccde7d14ae032911e86e5c4d2" kindref="member">EntityData::MODULE</ref><sp/>&amp;&amp;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>aUrl<sp/>==<sp/></highlight><highlight class="stringliteral">&quot;http://www.example.com/foobar&quot;</highlight><highlight class="normal">)<sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keywordflow">return</highlight><highlight class="normal"><sp/><ref refid="classzorba_1_1StreamResource_1aec38e33bd9b3f68aae28bc15c01af83e" kindref="member" tooltip="Public factory method from istream.">StreamResource::create</ref></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>(</highlight><highlight class="keyword">new</highlight><highlight class="normal"><sp/>std::istringstream</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>(</highlight><highlight class="stringliteral">&quot;module<sp/>namespace<sp/>lm<sp/>=<sp/>&apos;http://www.example.com/foobar&apos;;<sp/>&quot;</highlight><highlight class="normal"></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="stringliteral">&quot;declare<sp/>function<sp/>lm:foo()<sp/>{<sp/>&apos;foo&apos;<sp/>};&quot;</highlight><highlight class="normal">),<sp/>&amp;streamReleaser);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keywordflow">else</highlight><highlight class="normal"><sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keywordflow">return</highlight><highlight class="normal"><sp/>NULL;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>};</highlight></codeline>
</programlisting></para><para>A more realistic example would be a resolver that takes URLs (possibly with a non-standard scheme, such as <computeroutput>db:</computeroutput>) and loads the content for those URLs from a database. As long as the database API allows you to obtain the information as a <computeroutput>std::istream</computeroutput>, you may stream this data directly to Zorba.</para><para>Two notes about memory management: First, when a user-defined <computeroutput>URLResolver</computeroutput> returns a <computeroutput>Resource</computeroutput>, Zorba will take memory ownership of the <computeroutput>Resource</computeroutput>, and will free it when it is no longer needed. Second, when user code creates a <computeroutput>StreamResource</computeroutput>, the <computeroutput>StreamResource</computeroutput> assumes memory ownership of the <computeroutput>std::istream</computeroutput> that is wrapped. However, Zorba cannot free the <computeroutput>std::istream</computeroutput> itself, because it was instantiated inside user code rather than inside Zorba&apos;s own library. On Windows, in some circumstances, if a DLL deletes an object that was not instantiated inside that DLL, the application will crash. Therefore, the <computeroutput>StreamResource</computeroutput> factory function <computeroutput>create()</computeroutput> also takes a <computeroutput>StreamReleaser</computeroutput>, which is a function pointer. Zorba will call this function pointer, passing the <computeroutput>std::istream</computeroutput>, when it is no longer needed; the function is expected to free the <computeroutput>std::istream</computeroutput>.</para><para>Unlike <computeroutput>mapURI()</computeroutput>, it is acceptable for <computeroutput>resolveURL</computeroutput> to throw exceptions. A URL Resolver should throw an exception if it believes that it is canonical for the URL (that is, it &quot;should be able&quot; to resolve it) but had some error during the attempt to resolve it. However, because Zorba may be attempting to resolve a number of candidate URIs, any exceptions thrown from a URL Resolver will be caught and consumed by Zorba. It will never re-throw any of these exceptions. It will merely remember the message of the first exception (assuming that it extends <computeroutput>std::exception</computeroutput>). If and only if <emphasis>no</emphasis> URL Resolver ever returns a valid Resource, Zorba will then throw a new exception with the saved message from the first-thrown exception.</para></sect3>
</sect2>
<sect2 id="uriresolvers_1component">
<title>Component URI Mappers for modules</title>
<para>In XQuery, it is possible for a particular module to actually be implemented as a set of more than one <computeroutput>.xq</computeroutput> files. When a query imports the module&apos;s URI, the query processor is expected to provide some mechanism whereby multiple files will be loaded and combined to form the whole module definition.</para><para>Zorba uses <emphasis>Component URI Mappers</emphasis> to allow for this. The API is the same <computeroutput>URIMapper</computeroutput> class, and they are registered with the static context using the same <computeroutput>registerURIMapper()</computeroutput> method.</para><para>How does Zorba know which URI Mappers are intended to provide a set of URIs for the components of a module, and which are intended to provide a set of <emphasis>possible</emphasis> candidate URIs for other purposes? There is actually another method on the <computeroutput>URIMapper</computeroutput> class: <computeroutput>mapperKind()</computeroutput>. This method should return a value from the enumeration <computeroutput>URIMapper::Kind</computeroutput>. There are two possible values: COMPONENT and CANDIDATE. The default implementation of <computeroutput>URIMapper</computeroutput> returns CANDIDATE, so for normal URI Mappers there is no need to override this method.</para><para>When Zorba needs to resolve a URI for a module import, it first invokes all registered component URI mappers to form a set of <emphasis>component URIs</emphasis>. Then, it resolves <emphasis>each</emphasis> of these component URIs using the full URI resoltuion mechanism documented above - calling all candidate URI mappers and URL resolvers in turn. Assuming that it successfully loads a Resource for <emphasis>each</emphasis> component URI, it then assembles all of these Resources into the final, loaded module.</para><para>Here is an example of a component URI mapper, which tells Zorba to load two other URIs to form a complete module:</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="keyword">class<sp/></highlight><highlight class="normal">MyModuleURIMapper<sp/>:<sp/></highlight><highlight class="keyword">public</highlight><highlight class="normal"><sp/>URIMapper</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">public</highlight><highlight class="normal">:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">virtual</highlight><highlight class="normal"><sp/>~MyModuleURIMapper()<sp/>{}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">virtual</highlight><highlight class="normal"><sp/>URIMapper::Kind<sp/>mapperKind()<sp/>throw()<sp/>{<sp/></highlight><highlight class="keywordflow">return</highlight><highlight class="normal"><sp/>URIMapper::COMPONENT;<sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">virtual</highlight><highlight class="normal"><sp/></highlight><highlight class="keywordtype">void</highlight><highlight class="normal"><sp/>mapURI(</highlight><highlight class="keyword">const</highlight><highlight class="normal"><sp/><ref refid="classzorba_1_1String" kindref="compound" tooltip="The Zorba string class.">zorba::String</ref><sp/>aUri,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>EntityData<sp/></highlight><highlight class="keyword">const</highlight><highlight class="normal">*<sp/>aEntityData,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>std::vector&lt;zorba::String&gt;&amp;<sp/>oUris)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keywordflow">if</highlight><highlight class="normal"><sp/>(aEntityData-&gt;getKind()<sp/>!=<sp/>EntityData::MODULE)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keywordflow">return</highlight><highlight class="normal">;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keywordflow">if</highlight><highlight class="normal">(aUri<sp/>==<sp/></highlight><highlight class="stringliteral">&quot;http://www.example.com/mymodule&quot;</highlight><highlight class="normal">)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>oUris.push_back(</highlight><highlight class="stringliteral">&quot;http://www.example.com/mymodule/mod1&quot;</highlight><highlight class="normal">);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>oUris.push_back(</highlight><highlight class="stringliteral">&quot;http://www.example.com/mymodule/mod2&quot;</highlight><highlight class="normal">);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>};</highlight></codeline>
</programlisting></para><para>As mentioned, each of the component URIs will be treated to the full URI resolution mechanism, including Zorba&apos;s built-in mechanisms. So, given the code above and a default Unix installation, Zorba will proceed to attempt to load the following files:</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/>/usr/share/zorba-2.0.0/modules/com/example/www/mymodule/mod1.xq</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>/usr/share/zorba-2.0.0/modules/com/example/www/mymodule/mod2.xq</highlight></codeline>
</programlisting></para><para>If both are found, then the two together will be taken to form the complete definition for the module <computeroutput><ulink url="http://www.example.com/mymodule">http://www.example.com/mymodule</ulink></computeroutput>.</para></sect2>
<sect2 id="uriresolvers_1disallowing">
<title>Disallowing URIs</title>
<para>Sometimes, it might be required to forbid access to a certain URI. For example, a user might disallow access to the file module because she doesn’t want XQuery developers to access files stored locally on the machine that is running Zorba. (Remember that the built-in URL Resolver for <computeroutput>file:</computeroutput> URLs is implemented in terms of the file module, so doing this will effectively disable all file access - module and schema importing, loading documents, and so on.)</para><para>Therefore, the static context also provides a mechanism for preventing developers from importing particular modules. This is also handled with a <computeroutput>URIMapper</computeroutput> in a simple way: If <emphasis>any</emphasis> registered URI Mapper (candidate or component) ever returns the value <computeroutput>URIMapper::DENY_ACCESS</computeroutput> in the <computeroutput>oUris</computeroutput> vector, Zorba will immediately throw a &quot;URI access denied&quot; exception.</para><para>Here is an example URI Mapper which will suppress the loading of the file module:</para><para><programlisting><codeline><highlight class="normal"><sp/><sp/><sp/></highlight><highlight class="keyword">class<sp/></highlight><highlight class="normal">DenyFileAccessURIMapper<sp/>:<sp/></highlight><highlight class="keyword">public</highlight><highlight class="normal"><sp/>URIMapper</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">public</highlight><highlight class="normal">:</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">virtual</highlight><highlight class="normal"><sp/>~DenyFileAccessURIMapper()<sp/>{}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/></highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keyword">virtual</highlight><highlight class="normal"><sp/></highlight><highlight class="keywordtype">void</highlight><highlight class="normal"><sp/>mapURI(</highlight><highlight class="keyword">const</highlight><highlight class="normal"><sp/><ref refid="classzorba_1_1String" kindref="compound" tooltip="The Zorba string class.">zorba::String</ref><sp/>aUri,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>EntityData<sp/></highlight><highlight class="keyword">const</highlight><highlight class="normal">*<sp/>aEntityData,</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>std::vector&lt;zorba::String&gt;&amp;<sp/>oUris)</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/></highlight><highlight class="keywordflow">if</highlight><highlight class="normal">(aEntityData-&gt;getKind()<sp/>==<sp/>EntityData::MODULE<sp/>&amp;&amp;</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>aUri<sp/>==<sp/></highlight><highlight class="stringliteral">&quot;http://www.zorba-xquery.com/modules/file&quot;</highlight><highlight class="normal">)<sp/>{</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/><sp/>oUris.push_back(URIMapper::DENY_ACCESS);</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/><sp/><sp/>}</highlight></codeline>
<codeline><highlight class="normal"><sp/><sp/><sp/>};</highlight></codeline>
</programlisting> </para></sect2>
</sect1>
    </detaileddescription>
  </compounddef>
</doxygen>
