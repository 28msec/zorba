IF(WIN32)
  SET(PATH_SEP ";")
ELSE(WIN32)
  SET(PATH_SEP ":")
ENDIF(WIN32)

SET(SECONDARY_MODULE_PATHS "${SECONDARY_MODULE_PATHS}${PATH_SEP}@CMAKE_SOURCE_DIR@/src/${PATH_SEP}@CMAKE_BINARY_DIR@/src/")
