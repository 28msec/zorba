With Ubuntu 10.10
As of 18-jan-2011
gcc version 4.4.5
----------------------------------------


sudo apt-get update
sudo apt-get install subversion
sudo apt-get install build-essential
sudo apt-get install valgrind
sudo apt-get install cmake

sudo apt-get install libxml2-dev
sudo apt-get install libxslt-dev
sudo apt-get install libicu-dev
sudo apt-get install libcurl4-openssl-dev
sudo apt-get install libtidy-dev
sudo apt-get install libxerces-c-dev
sudo apt-get install iconv
sudo apt-get install libzlib-dev



--Build IMAP c-client

wget ftp://ftp.cac.washington.edu/imap/imap-2007e.tar.gz
tar -xf imap-2007e.tar.gz
sudo apt-get install libpam-dev
cd imap-2007e
make lnp EXTRACFLAGS="-I/usr/include/openssl -fPIC"
(if something goes wrong, delete all, tar again and make again, don't trust make clean)

Or another option is to install the c-client lib from ppa:juan457/zorba-dependencies
see http://launchpad.net/~juan457/+archive/zorba-dependencies/+packages for details. 


--Install ImageMagick (for image modules)

sudo apt-get install imagemagick
sudo apt-get install libmagick++-dev




-- Install GEOS (for geo module)

sudo apt-get install libgeos-dev
Or if the version is older than 3.2.2 then compile manually:
sudo apt-get remove libgeos-dev
svn checkout http://svn.osgeo.org/geos/trunk geos-svn
cd geos-svn
mkdir build
cd build
cmake ../
make
sudo make install




-- Install Jansson (for JSON module)  from ppa:petri/ppa

sudo add-apt-repository ppa:petri/ppa
sudo apt-get update
sudo apt-get install libjansson4 libjansson-dev




--Get and build zorba

svn co https://zorba.svn.sourceforge.net/svnroot/zorba/trunk/zorba
cd zorba
mkdir debug
cd debug
cmake -DCMAKE_BUILD_TYPE="Debug" -DCCLIENT_INCLUDE="~/imap-2007e/c-client" -DCCLIENT_LIBRARY="~/imap-227e/c-client/c-client.a" ../
make
