@ECHO OFF

rem This script is used to execute Zorba executables without setting the PATH environment variable manually.
rem Note: This script closes cmd.exe if an error happens!
rem Example: win_exec.bat bin\zorba.exe -q "1+2"

SETLOCAL
SET PATH=@WIN_CMAKE_BINARY_DIR@\src@MSVC_IDE_PATH_SUFFIX@;@ZORBA_REQUIRED_DLLS_PATH_NATIVE@;@ZORBA_REQUIRED_DLL_PATHS@;%PATH%
%*
IF %ERRORLEVEL% neq 0 (
  EXIT %ERRORLEVEL%
)
ENDLOCAL
