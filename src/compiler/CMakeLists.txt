SET(COMPILER_SRCS)
SET(COMPILER_BUILD_SRCS)

INCLUDE (compiler/parser/CMakeLists.txt)
FOREACH (SRCS ${PARSER_SRCS})
    LIST(APPEND COMPILER_SRCS parser/${SRCS})
ENDFOREACH (SRCS)

FOREACH (SRCS ${PARSER_BUILD_SRCS})
    LIST(APPEND COMPILER_BUILD_SRCS ${SRCS})
ENDFOREACH (SRCS)

INCLUDE (compiler/parsetree/CMakeLists.txt)
FOREACH (SRCS ${PARSETREE_SRCS})
    LIST(APPEND COMPILER_SRCS parsetree/${SRCS})
ENDFOREACH (SRCS)


INCLUDE (compiler/translator/CMakeLists.txt)
FOREACH (SRCS ${TRANSLATOR_SRCS})
    LIST(APPEND COMPILER_SRCS translator/${SRCS})
ENDFOREACH (SRCS)


INCLUDE (compiler/codegen/CMakeLists.txt)
FOREACH (SRCS ${CODEGEN_SRCS})
    LIST(APPEND COMPILER_SRCS codegen/${SRCS})
ENDFOREACH (SRCS)

INCLUDE (compiler/expression/CMakeLists.txt)
FOREACH (SRCS ${EXPRESSION_SRCS})
    LIST(APPEND COMPILER_SRCS expression/${SRCS})
ENDFOREACH (SRCS)
