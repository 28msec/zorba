SET(RUNTIME_SRCS
    accessors/AccessorsImpl.cpp
    core/item_iterator.cpp
    numerics/NumericsImpl.cpp
    strings/StringsImpl.cpp
    booleans/BooleanImpl.cpp
    dateTime/DurationsDatesTimes.cpp
    paths/path_iterators.cpp
    base/iterator.cpp
    sequences/SequencesImpl.cpp)

HEADER_GROUP_SUBFOLDER(accessors)
HEADER_GROUP_SUBFOLDER(core)
HEADER_GROUP_SUBFOLDER(numerics)
HEADER_GROUP_SUBFOLDER(strings)
HEADER_GROUP_SUBFOLDER(booleans)
HEADER_GROUP_SUBFOLDER(dateTime)
HEADER_GROUP_SUBFOLDER(paths)
HEADER_GROUP_SUBFOLDER(base)
HEADER_GROUP_SUBFOLDER(sequences)
